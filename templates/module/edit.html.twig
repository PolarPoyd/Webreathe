{# module/edit.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Edit Module{% endblock %}

{% block body %}

{% for message in app.flashes('danger') %}
<div class="alert alert-danger mt-4">
    {{ message }}
</div>
{% endfor %}

<h1>Edit Module</h1>

{{ form_errors(form) }}

{{ form_start(form) }}
{{ form_widget(form) }}

<button class="btn btn-primary">Modifier</button>
{{ form_end(form) }}

<a href="{{ path('app_module_index') }}">Retour à la liste</a>

{{ include('module/_delete_form.html.twig') }}
{% endblock %}
